[package]
name = "thrustr-plugin"
version = "0.1.0"
edition = "2024"

[dependencies]
domain.workspace = true
wit-bindgen = "0.51"

[package.metadata.component]
package = "thrustr:storefront"

[lib]
crate-type = ["cdylib", "rlib"]

package thrustr:storefront;

interface kv-store {
    variant error {
        not-found,
        internal(string),
    }

    get: func(key: string) -> result<list<u8>, error>;
    set: func(key: string, value: list<u8>) -> result<_, error>;

}

interface storefront-provider {
    init: func() -> result<_, string>;
}

world storefront {
    import kv-store;
    export storefront-provider;
}

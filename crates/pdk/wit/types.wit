package thrustr:plugin@0.1.0;

interface types {
    variant error {
        config(string),
        auth(string),
        other(string),
    }

    record game {
        name: string,
        lookup-id: string,
        external-ids: list<tuple<string, string>>,
    }
}
